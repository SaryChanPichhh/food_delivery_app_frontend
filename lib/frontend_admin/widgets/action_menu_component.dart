import 'package:flutter/material.dart';
import 'package:pull_down_button/pull_down_button.dart';

class ActionMenuComponent extends StatelessWidget {
  final List<String> actionsMenu;

  const ActionMenuComponent({super.key, required this.actionsMenu});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
